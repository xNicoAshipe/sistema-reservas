{
  "name": "sistema-reservas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tests/validate-artifact.mjs",
    "lint": "node --check worker.js && node --check route-map.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
